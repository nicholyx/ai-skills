我需要在这些代码仓库中实现 [功能]：[仓库列表]。首先对所有仓库进行分析，了解当前现状。然后组建一个团队，每个成员负责一个仓库。每位成员处理各自仓库的修改，团队负责人协调各仓库之间的接口契约。先从分析工作开始，随后提出任务拆解方案，再进行开发实施。

你正在统筹这个低代码平台的跨仓库功能开发工作。各仓库路径如下：[路径列表]。
首先，为每个仓库**并行启动一个分析子代理**，每个子代理需完成以下任务：
1. 梳理该仓库的架构；
2. 定位所有与[功能]相关的文件；
3. 记录仓库之间的接口与契约。

待所有子代理返回结果后，整合生成一份**统一的变更方案**，并明确依赖顺序——无跨依赖的仓库可并行修改。
为每个仓库的变更创建 Git 工作树。
按照依赖顺序执行代码修改，每一批次修改完成后运行测试。
标记仓库之间存在冲突或不匹配的接口契约。
在执行任何提交前，生成一份最终的跨仓库差异汇总供我审核。
