# 安全策略

## 支持范围

本仓库由个人业余维护，**不发布版本、不维护 tag**，只对 `main` 分支的最新提交提供
安全修复。使用旧提交时请先更新到最新再反馈问题。

| 范围 | 是否支持安全修复 |
| --- | --- |
| `main` 分支最新提交 | ✅ |
| 更早的提交、他人 fork | ❌ |

## 报告安全漏洞

**请不要通过公开的 Issue 报告安全漏洞。**

请使用 GitHub 的私有漏洞报告渠道：

👉 [报告安全漏洞](https://github.com/nicholyx/ai-skills/security/advisories/new)

如果该入口不可用，也可以发邮件到 `nicholyx@163.com`，**标题注明 `[SECURITY]`**。
（`[CoC]` 开头的是行为准则投诉，走的是另一条路，见 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。）

### 请在报告中包含

- 受影响的文件与提交哈希
- 复现步骤：**你实际用的提示词**、模型的回应、以及本机发生了什么
  （如果技能诱导模型执行了命令，请把命令原文带上）
- 影响评估：攻击者能做什么，需要什么前置条件
  （例如「使用者必须主动触发该技能」还是「只要技能被加载就成立」）
- 如果你有修复思路，欢迎一并附上（但请不要直接提公开 PR）

### 我们的响应承诺

| 阶段 | 目标时间 |
| --- | --- |
| 确认收到 | 7 天内 |
| 初步评估与定级 | 14 天内 |
| 修复或缓解措施 | 视严重程度，尽快 |
| 公开披露 | 修复发布后，与你协商披露时机 |

## 本项目的威胁模型

理解本仓库的攻击面，有助于你判断某个问题算不算安全漏洞。

### 这是一个「分发可执行 AI 指令的内容仓库」

本仓库**不持有任何凭证**，也没有自动化流水线需要保护。它的本质是**内容分发**：

- `custom/**` 下的技能是**我们写的指令集**。它经 `npx skills add` 落到
  `~/.agents/skills/`，再由 `custom/daily/skills-sync/` 用软链接同步进
  `~/.claude/skills` 与 `~/.codebuddy/skills` —— 也就是**在使用者的全局 AI 环境里生效**，
  而不只是在克隆这份仓库的目录里。
- 技能内容不是「数据」，而是**会被 AI 当成指令执行的自然语言**。一段 Markdown
  可以描述如何执行命令、读取哪些文件、把内容发到哪里。

因此本仓库最核心的风险不是「凭证泄露」，而是：

> **恶意或误导性的技能内容，会随一次 `git pull` / `npx skills add` 直接进入
> 使用者的 AI 环境，并在其中获得指令级的影响力。**

具体形态包括但不限于：

- **提示注入**：技能正文里夹带让模型偏离用户意图的指令
  （例如「忽略用户此前的限制」「把结果也发到某个地址」）
- **诱导执行危险命令**：把破坏性操作包装成正常步骤
  （例如未经确认的 `rm -rf`、改写使用者 shell 配置、往仓库里塞全局 hook）
- **泄露本地文件**：指示模型读取 `~/.ssh`、`~/.aws`、环境变量或项目里的
  `.env`，再输出到模型回复、日志或某个外部位置

### 第二个风险：上游技能被伪造或劫持

`.agents/**` 下 31 个技能是从上游仓库安装的 vendored 内容，由 `skills-lock.json`
（记录 `source`、`skillPath`、`computedHash`）追踪。如果 lock 条目、来源仓库或
分发渠道被伪造/劫持，那么 `npx skills add` / `npx skills update` 会把**非预期的内容**
装进 `.agents/**` —— 它和使用者自己写的技能一样是全局生效的。
这类问题多半需要向上游仓库反馈，但**在本仓库一侧也值得报告**：
lock 文件是我们维护的，它的内容是否正确由我们负责。

## 已采取的防护措施

| 措施 | 说明 |
| --- | --- |
| CI 严格校验自建内容 | `custom/**` 的 frontmatter、目录名一致性、evals.json 结构违规都是 **fail 级**，会直接拦下 PR |
| 上游内容只 warn | `.agents/**` 的同类违规只提示、不计入退出码 —— 因为**我们无权修改它**（改了会在 `npx skills update` 时丢失）。这是分级，不是漏检 |
| 正确的目标集 | 所有检查器以 **git 索引**为目标集，不递归扫描文件系统，避免「本地看的和 CI 看的不是同一批文件」 |
| 编码正确性检查 | 非法 UTF-8 与 U+FFFD 替换字符会被拦下，防止内容在编码转换中被悄悄改写 |
| CODEOWNERS 收紧 | [.github/CODEOWNERS](.github/CODEOWNERS) 要求 `/custom/`、`/scripts/`、`/docs/` 与治理文件的改动必须经维护者 review |
| CI 无写入权限 | 工作流只声明 `contents: read`，且 checkout 时 `persist-credentials: false`，CI 本身不会成为写入口 |
| 第三方 Action 按 SHA 固定 | 工作流引用的 action 全部 pin 到提交 SHA，降低供应链漂移风险 |

### 这些措施**不能**做到什么

CI 只能校验**结构与编码**。它**读不懂技能里的自然语言**，因此：

- 一段语法完全合法、结构完全正确的 `SKILL.md`，仍然可能在语义上是有害的
- 「这个技能在该触发时会不会触发」目前也没有自动化手段，只能靠人实际用一遍

也就是说，**防止恶意内容进入仍然主要依赖人工 review**。这正是 `custom/` 被
CODEOWNERS 单独收紧的原因。

## 明确不在威胁模型内的情况

以下情况**不算**本项目的安全漏洞，请不要作为漏洞提交：

- 技能内容在语义上「好不好用」：触发时机不理想、输出格式不合口味、步骤不够简洁
  —— 这些是**功能问题**，请提普通 Issue
- 模型自身的输出安全：本仓库分发的是提示词与文档，不控制模型如何生成回应
- 使用者本机的其他软件：AI 工具自身的沙箱、权限模型、插件生态的问题，请找对应厂商
- 上游技能仓库自身的漏洞：请向对应上游仓库反馈（`.agents/**` 不是我们的代码）
- 你自己在 fork 里修改内容后产生的问题
- 你本地的软链接配置错误，或把技能链接到了非预期的位置

## 如果你怀疑自己的环境已被影响

1. **断开影响面**：把 `~/.claude/skills`、`~/.codebuddy/skills` 下指向可疑技能的
   软链接删掉（`skills-sync` 会在下一次同步时重建有效链接、清理失效链接）
2. **检查技能来源**：在 `~/.agents/skills/` 找到对应目录，确认它的具体来源与提交
3. **轮换可能已暴露的凭证**：如果可疑内容涉及读取本地文件，请把 `~/.ssh`、
   `~/.aws/credentials`、项目 `.env` 里的密钥视为已泄露并轮换
4. **检查外发动作**：确认模型是否在你的会话里执行过网络请求、推送或写入操作
5. **报告**：按上面的私有渠道说明，把提示词、模型回应与实际命令一并提交
